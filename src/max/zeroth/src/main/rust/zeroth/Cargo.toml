[package]
name = "zeroth"
version = "0.0.0-SNAPSHOT"
edition = "2021"
publish = false

[dependencies]
clap = { version = "4", features = ["derive"] }
humantime = "2"

[profile.release]
strip = true
lto = true
