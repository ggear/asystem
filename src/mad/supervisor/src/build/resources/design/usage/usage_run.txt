Usage:
  supervisor run [OPTIONS]

Description:
  Run the supervisor process to collect and publish system statistics and
  perform supervisory duties.

Options:
  -p, --poll-period duration
      Polling interval, with unit suffixes [s, m, h] (default: 5s).

  -s, --snaphot-period duration
      Snapshot interval, with unit suffixes [s, m, h] (default: 5m).

  -h, --help
      Display this help message and exit.