version: '3.7'
services:
  anode:
    container_name: anode
    image: anode:${VERSION}
    init: true
    env_file: .env
    environment:
      - TZ=Australia/Perth
      - LOCAL_IP
    ports:
      - '8091:8091'
    volumes:
      - '${DATA_DIR}:/etc/anode'
