host: ${ANODE_IP}
port: ${ANODE_PORT}
plugin:
  fronius:
    poll_seconds: 1
    buffer_ticks: 10
    history_partitions: 50
    history_ticks: 100000000000000
    history_partition_seconds: 1
    repeat_seconds: -1
