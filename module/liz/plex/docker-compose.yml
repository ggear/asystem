# https://hub.docker.com/r/plexinc/pms-docker/tags
version: '3.7'
services:
  plex:
    container_name: plex
    image: plexinc/pms-docker:1.29.1.6316-f4cdfea9c
    init: true
    restart: ${SERVICE_RESTART}
    environment:
      - TZ=Australia/Perth
      - PLEX_CLAIM=claim-sZFJxzAkZq3KxSJqhuUY
    volumes:
      - ${SERVICE_DATA_DIR}/config:/config
      - ${SERVICE_DATA_DIR}/transcode:/transcode
      - /data/media/Series:/media/series
    network_mode: host
    deploy:
      resources:
        limits:
          memory: 5G